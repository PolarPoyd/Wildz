{% extends 'base.html.twig' %}

{% block title %}CatRace{% endblock %}

{% block body %}
    <h1>CatRace</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_race.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cat_race.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ cat_race.createdAt ? cat_race.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cat_race_index') }}">back to list</a>

    <a href="{{ path('app_cat_race_edit', {'id': cat_race.id}) }}">edit</a>

    {{ include('pages/cat_race/_delete_form.html.twig') }}
{% endblock %}
