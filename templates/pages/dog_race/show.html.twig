{% extends 'base.html.twig' %}

{% block title %}DogRace{% endblock %}

{% block body %}
    <h1>DogRace</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dog_race.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dog_race.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ dog_race.createdAt ? dog_race.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dog_race_index') }}">back to list</a>

    <a href="{{ path('app_dog_race_edit', {'id': dog_race.id}) }}">edit</a>

    {{ include('pages/dog_race/_delete_form.html.twig') }}
{% endblock %}
